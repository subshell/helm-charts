apiVersion: v2
name: sophora-media-finder
description: A Helm chart for Sophora Media Finder
type: application
version: 0.2.2
appVersion: "5.x"

annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Add option to set CORS origin for the application