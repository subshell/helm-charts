apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-gke.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-gke.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gateway-gke.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
