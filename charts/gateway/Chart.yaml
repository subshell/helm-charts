apiVersion: v2
name: gateway
description: A Helm chart to deploy a Gateway. Optionally use cert-manager to fetch Let's Encrypt certificates.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 1.0.3
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: "more compact secret name"
    - kind: changed
      description: "listener prefix ends in underscore _ now"
    - kind: changed
      description: "ReadMe with simpler examples"
