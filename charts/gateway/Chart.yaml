apiVersion: v2
name: gateway
description: A Helm chart to deploy a Gateway. Optionally use cert-manager to fetch Let's Encrypt certificates.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 1.0.4
annotations:
  artifacthub.io/changes: |
    - kind: fixed
      description: "use host as listener name, as it must be a valid domain name"
    - kind: changed
      description: "quote all used strings from values"
