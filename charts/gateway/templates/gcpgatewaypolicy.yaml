{{- $gp := .Values.gcp.gatewayPolicy -}}
{{- if $gp -}}
apiVersion: networking.gke.io/v1
kind: GCPGatewayPolicy
metadata:
  name: {{ required "gcp.gatewayPolicy.name is required" $gp.name | quote }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
spec:
  {{- with $gp.default }}
  default:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: {{ include "gateway.fullname" . }}
{{- end -}}
