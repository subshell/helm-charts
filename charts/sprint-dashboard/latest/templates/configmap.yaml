apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sprint-dashboard.fullname" . }}-config
data:
  application.yaml: |- {{ toYaml (required "A valid application.yml sprint-dashboard config is required (nested in the value .Values.config)!" .Values.config) | nindent 4 }}