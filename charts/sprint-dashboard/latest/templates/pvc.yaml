apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "sprint-dashboard.fullname" . }}-db
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.db.size | default "256Mi" }}