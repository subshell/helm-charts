apiVersion: v1
kind: Service
metadata:
  name: {{ include "sprint-dashboard.fullname" . }}
  labels:
    {{- include "sprint-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sprint-dashboard.selectorLabels" . | nindent 4 }}
