apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "artifactory-cleanup.configName" . }}
  labels:
  {{- include "artifactory-cleanup.labels" . | nindent 4 }}
data:
  artifactory-cleanup.yaml: |- {{ toYaml (required "A configuration is required" .Values.cleanup.config) | nindent 4 }}
