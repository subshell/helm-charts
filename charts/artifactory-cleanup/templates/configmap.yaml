apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "artifactory-cleanup.configName" . }}
  labels:
  {{- include "artifactory-cleanup.labels" . | nindent 4 }}
data:
  artifactory-cleanup.yaml: | {{ required "A configuration is required" .Values.cleanup.configYaml | toYaml | nindent 4 }}
