apiVersion: v1
kind: Service
metadata:
  name: {{ include "sophora-dashboard-old.fullname" . }}
  labels:
    {{- include "sophora-dashboard-old.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sophora-dashboard-old.selectorLabels" . | nindent 4 }}
